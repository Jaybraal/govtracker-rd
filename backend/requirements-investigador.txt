# Dependencias mínimas para el cron diario (scripts/investigar.py) y su
# suite de tests — NO usar requirements.txt completo en CI: ese archivo
# tiene un conflicto de versiones preexistente (spacy vs chromadb vs
# fastapi-cli, ninguno usado por el camino del investigador) que rompe
# `pip install` en un entorno limpio. Ver Task 2 del plan
# docs/superpowers/plans/2026-07-15-investigador-autonomo.md.
sqlalchemy==2.0.30
pydantic==2.7.1
pydantic-settings==2.2.1
python-dotenv==1.0.1
httpx==0.27.0
beautifulsoup4==4.12.3
lxml==5.2.1
loguru==0.7.2
tenacity==8.3.0
pytest==8.2.2
pytest-mock==3.14.0
pytest-asyncio==0.23.7
